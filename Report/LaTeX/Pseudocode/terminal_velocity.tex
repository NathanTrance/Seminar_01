\begin{algorithm}[t]
\caption{Terminal Velocity Matching (TVM) Training}
\label{alg:terminal_velocity}
\begin{algorithmic}[1]
\Require Data $x$, noise $\epsilon$, displacement network $F_\theta$, velocity network $v_\theta$, optimizer
\State Sample $t, s \sim \text{Uniform}(0,1)$
\State $z_t \gets (1 - t) \, x + t \, \epsilon$; \quad $z_s \gets (1 - s) \, x + s \, \epsilon$
\State $v_s \gets \epsilon - x$ \Comment{Conditional velocity at $s$}
\State $f_{t,s} \gets (s - t) \, F_\theta(z_t, t, s)$ \Comment{Displacement map}
\State Compute JVP: $\frac{d}{ds} F_\theta(z_t, t, s)$
\State $\text{term\_vel} \gets F_\theta(z_t, t, s) + (s - t) \cdot \frac{d}{ds} F_\theta$ \Comment{Terminal velocity}
\State $z_{\text{arrived}} \gets z_t + f_{t,s}$ \textbf{detach}
\State $v_{\text{tgt}} \gets v_\theta(z_{\text{arrived}}, s)$ \textbf{detach}
\State $\mathcal{L}_{\text{TV}} \gets \| \text{term\_vel} - v_{\text{tgt}} \|^2$ \Comment{Terminal velocity loss}
\State $v_{\text{pred}} \gets v_\theta(z_s, s)$
\State $\mathcal{L}_{\text{FM}} \gets \| v_{\text{pred}} - v_s \|^2$ \Comment{Flow matching loss}
\State $\mathcal{L} \gets \mathcal{L}_{\text{TV}} + \mathcal{L}_{\text{FM}}$
\State Update $\theta$ with $\nabla_\theta \mathcal{L}$
\State \Return $\mathcal{L}$
\end{algorithmic}
\end{algorithm}
